{
  "name": "momopick",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run build --prefix web && npm run preview --prefix web"
  }
}
